apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  MONGODB_URI: {{ .Values.configmap.data.MONGODB_URI }}
  PORT: {{ .Values.configmap.data.PORT }}
  MONGO_INITDB_DATABASE: {{ .Values.configmap.data.MONGO_INITDB_DATABASE }}
